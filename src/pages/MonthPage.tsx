export function MonthPage() {
  return (
    <div className="card">
      <p style={{ color: "var(--text-muted)" }}>
        Month view is no longer in the main nav. Use Plan for the week calendar.
      </p>
    </div>
  );
}
